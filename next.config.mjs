/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // Ajoutez ce domaine pour autoriser les images de Google
    },
};

export default nextConfig;


