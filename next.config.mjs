/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "images.unsplash.com"], // Combinez les domaines ici
  },
};

export default nextConfig;
